import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

/**Requirements:
 * Basic structure -header, nav, main, footer
 * 2. Thumbnails - text, price description 
 * 3. responsive features
 * 
 */